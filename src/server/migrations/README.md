# Calendar migrations live here. Empty in the scaffold (#183) — opening the
# DB only creates schema_migrations. The events table is added by #186 as the
# first migration: YYYYMMDDHHMMSS_slug.ts exporting `name` + `up(db)`.
